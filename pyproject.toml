[project]
name = "job-alert"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-jobspy>=0.1.0",
    "requests>=2.31.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = []
